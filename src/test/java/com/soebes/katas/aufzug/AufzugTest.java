package com.soebes.katas.aufzug;

import static org.testng.Assert.assertFalse;
import static org.testng.Assert.assertTrue;

import org.testng.annotations.Test;

public class AufzugTest {
    public class Aufzug {
      private String name;
      private boolean inBewegung;
    }

    @Test
    public void regelnLaden() {
        //Aufzug (1-n) bedient alle Etagen (eventuell später verfeinern)
        //Aufzug(e)
        // - Fahrtwunsch (auf jeder Etag wird ein Stockwerk als Ziel eingegeben) (nicht im Aufzug)
        // - Anzahl Etagen (Noch festlegen)
        // - Fahrtrichtung (bewegt sich nach oben/unten)
        // - Ist auf Etage X angekommen
        //
    }
}
